<p id="notice"><%= notice %></p>

<h1>Listing Bookmarks</h1>

<table>
  <thead>
    <tr>
      <th>Url</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @bookmarks.each do |bookmark| %>
      <tr>
        <td><%= bookmark.url %></td>
        <td><%= link_to 'Show', [@topic, bookmark] %></td>
        <td><%= link_to 'Edit', edit_topic_bookmark_path(@topic, bookmark) %></td>
        <td><%= link_to 'Destroy', [@topic, bookmark], method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Bookmark', new_topic_bookmark_path(@topic) %>
